namespace CredibillMauiApp;

public static class Constants
{
    public const string BaseApiUrl = "https://my-api-url/api/"; // ← Change à ton API
    // Toggle to avoid calling the API for authentication (login/refresh)
    public const bool UseApiAuth = false;
}
