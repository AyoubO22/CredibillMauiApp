<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CredibillMauiApp.Views.Payments.PaymentsPage"
             Title="Payments">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackLayout Grid.Row="0" Padding="20,10,20,0" Spacing="8">
            <Label Text="Payments" Style="{StaticResource TitleLargeStyle}" HorizontalOptions="Center" />
            <SearchBar Placeholder="Search..." Text="{Binding SearchText}" Margin="0,6,0,0" />
        </StackLayout>
        <RefreshView Grid.Row="1" IsRefreshing="{Binding IsBusy}" Command="{Binding LoadAsyncCommand}">
            <CollectionView ItemsSource="{Binding FilteredPayments}" SelectionMode="Single" SelectionChanged="OnPaymentSelected" Margin="10">
                <CollectionView.ItemsLayout>
                    <GridItemsLayout Orientation="Vertical" Span="1" />
                </CollectionView.ItemsLayout>
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource CardBorderStyle}">
                            <VerticalStackLayout>
                                <Label Text="{Binding Reference, StringFormat='Ref: {0}'}" FontSize="18" FontAttributes="Bold" TextColor="{AppThemeBinding Light=#222222, Dark=White}" />
                                <Label Text="{Binding Amount, StringFormat='{0}€ payment'}" FontSize="14" TextColor="{AppThemeBinding Light=#666666, Dark=#D1D1D6}" />
                                <Label Text="{Binding DatePaid, StringFormat='Date: {0:dd/MM/yyyy}'}" FontSize="12" TextColor="{AppThemeBinding Light=#999999, Dark=#A1A1A6}" />
                            </VerticalStackLayout>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </RefreshView>
        <Button Text="+" Command="{Binding AddCommand}" Style="{StaticResource PrimaryActionButton}" FontSize="28" WidthRequest="60" HeightRequest="60" CornerRadius="30" VerticalOptions="End" HorizontalOptions="End" Margin="20" />
    </Grid>
</ContentPage>
