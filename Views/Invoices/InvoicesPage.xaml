<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CredibillMauiApp.Views.Invoices.InvoicesPage"
             Title="Invoices">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackLayout Grid.Row="0" Padding="20,10,20,0">
            <Label Text="Factures" FontSize="28" FontAttributes="Bold" TextColor="#2D5CFE" HorizontalOptions="Center" />
            <SearchBar Placeholder="Rechercher..." Text="{Binding SearchText}" Margin="0,10,0,0" />
        </StackLayout>
        <RefreshView Grid.Row="1" IsRefreshing="{Binding IsBusy}" Command="{Binding LoadCommand}">
            <CollectionView ItemsSource="{Binding FilteredInvoices}" SelectionMode="Single" SelectionChanged="OnInvoiceSelected" Margin="10">
                <CollectionView.ItemsLayout>
                    <GridItemsLayout Orientation="Vertical" Span="1" />
                </CollectionView.ItemsLayout>
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Border Stroke="#E0E3EA" StrokeThickness="1" Background="#F5F7FB" Margin="0,5" Padding="15" StrokeShape="RoundRectangle 12">
                            <VerticalStackLayout>
                                <Label Text="Facture #{Binding Id}" FontSize="18" FontAttributes="Bold" TextColor="#222" />
                                <Label Text="Montant: {Binding Amount}" FontSize="14" TextColor="#666" />
                                <Label Text="Date: {Binding DateIssued, StringFormat='dd/MM/yyyy'}" FontSize="12" TextColor="#999" />
                            </VerticalStackLayout>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </RefreshView>
        <Button Text="+" Command="{Binding AddCommand}" BackgroundColor="#2D5CFE" TextColor="White" FontSize="32" WidthRequest="60" HeightRequest="60" CornerRadius="30" VerticalOptions="End" HorizontalOptions="End" Margin="20" />
    </Grid>
</ContentPage>