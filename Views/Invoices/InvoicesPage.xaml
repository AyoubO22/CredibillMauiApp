<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CredibillMauiApp.Views.Invoices.InvoicesPage"
             Title="Invoices">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackLayout Grid.Row="0" Padding="20,10,20,0" Spacing="8">
            <Label Text="Invoices" Style="{StaticResource TitleLargeStyle}" HorizontalOptions="Center" />
            <Picker Title="Filter by customer" ItemsSource="{Binding Customers}" ItemDisplayBinding="{Binding Name}" SelectedItem="{Binding SelectedCustomer}" />
            <SearchBar Placeholder="Search..." Text="{Binding SearchText}" Margin="0,6,0,0" />
        </StackLayout>
        <RefreshView Grid.Row="1" IsRefreshing="{Binding IsBusy}" Command="{Binding LoadAsyncCommand}">
            <CollectionView ItemsSource="{Binding FilteredInvoices}" SelectionMode="Single" SelectionChanged="OnInvoiceSelected" Margin="10">
                <CollectionView.ItemsLayout>
                    <GridItemsLayout Orientation="Vertical" Span="1" />
                </CollectionView.ItemsLayout>
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource CardBorderStyle}">
                            <Grid ColumnDefinitions="*,Auto" RowDefinitions="Auto,Auto">
                                <!-- Left: title and date -->
                                <Label Grid.Row="0" Grid.Column="0" Text="{Binding Id, StringFormat='Invoice #{0}'}" FontSize="18" FontAttributes="Bold" TextColor="{AppThemeBinding Light=#222222, Dark=White}" />
                                <Label Grid.Row="1" Grid.Column="0" Text="{Binding DateIssued, StringFormat='Date: {0:dd/MM/yyyy}'}" FontSize="12" TextColor="{AppThemeBinding Light=#999999, Dark=#D1D1D6}" />
                                <!-- Right: amount aligned right -->
                                <Label Grid.Row="0" Grid.RowSpan="2" Grid.Column="1" Text="{Binding Amount, StringFormat='€{0:0.##}'}" FontSize="18" FontAttributes="Bold" TextColor="{AppThemeBinding Light=#222222, Dark=White}" HorizontalOptions="End" VerticalOptions="Center" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </RefreshView>
        <Button Text="+" Command="{Binding AddCommand}" Style="{StaticResource PrimaryActionButton}" FontSize="28" WidthRequest="60" HeightRequest="60" CornerRadius="30" VerticalOptions="End" HorizontalOptions="End" Margin="20" />
    </Grid>
</ContentPage>
